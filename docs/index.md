# Индекс

Файл пересобирается командой `/update-index` на основе фактического состава `docs/atomic-actions/` и `docs/test-cases/`. Не редактируется вручную.

*Не заполнено. Выполните `/update-index`.*
