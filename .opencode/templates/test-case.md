# TC-<CODE>-<NNN> — <Название>

## Код

`TC-<CODE>-<NNN>`

## Описание

<Одно бизнес-условие, проверяемое сценарием.>

## Тестовые данные

<Организовать данные именованными группами (`###`). Каждая группа — один из трёх типов.>

### <Название статической группы>

<Значения известны заранее.>

- **<Поле>** — `<значение>`.
- **<Поле>** — пусто.

### <Название динамической группы>

<Значения формируются по ходу сценария. Не указывать номер шага — только AA-источник, чтобы не зависеть от нумерации при правках.>

Формируется при выполнении `AA-<CODE>`. Значения не задаются заранее — создаются исполнением сценария.

### <Название генерируемой группы> (генерируется)

<Значения формирует генератор данных (например, Faker), способ реализации выбирается свободно на этапе `/implement-tc`, в документе не фиксируется. Формат/правила генерации указываются только для этого типа группы — нигде больше.>

- **<Поле>** — <формат и правила генерации>.
- **<Поле>** — <формат и правила генерации>.

<Если название поля в группе совпадает с названием заполняемого или проверяемого поля формы/параметра AA — в шаге сценария ссылаться на группу одной фразой, поля по отдельности не расписывать.>

## Зависимости

- `<AA-ID>` — <короткое мнемоническое название, используемое в шагах этого TC>.

<Название придумывается при создании TC и должно дословно совпадать с тем, как AA называется в разделе «Описание сценария». Не описание назначения AA — короткое имя для ссылки. Если зависимостей нет — указать строго: «Зависимости отсутствуют.»>

## Описание сценария

<Каждый шаг, вызывающий AA, оформлять по формату: `Выполнить <Мнемоническое название> (`<AA-ID>`), используя <ссылка на группу тестовых данных>.` Код AA указывать при каждом вызове, включая повторные вызовы одной и той же AA в одном сценарии. Не расписывать в шаге отдельные поля/значения — вся информация о данных уже зафиксирована в разделе «Тестовые данные».>

1. Выполнить <Мнемоническое название> (`<AA-ID>`), используя <ссылка на группу тестовых данных>.
2. <Обычный шаг без вызова AA.>

## Условие проверки

<Однозначный ожидаемый бизнес-результат.>
