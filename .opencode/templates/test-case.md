# TC-<CODE>-<NNN> — <Название>

## Код

`TC-<CODE>-<NNN>`

## Описание

<Одно бизнес-условие, проверяемое сценарием.>

## Роль

<Если TC начинается с уже подготовленной сессии — укажи роль из `docs/roles.md`. Если TC выполняет собственную аутентификацию как часть сценария (см. «Тестовые данные» и «Описание сценария») — укажи строго «Роль не используется.».>

`<ROLE-CODE>` — <Название роли>.

## Тестовые данные

<Организовать данные именованными группами (`###`). Каждая группа — один из двух типов.>

### <Название статической группы>

<Значения известны заранее.>

- **<Поле>** — `<значение>`.
- **<Поле>** — пусто.

### <Название динамической группы>

<Значения формируются по ходу сценария — через выполнение обычного Action или Action-генератора данных (см. `documentation-authoring.md`). Не указывать номер шага — только источник (`AA-<CODE>`), чтобы не зависеть от нумерации при правках.>

Формируется при выполнении `AA-<CODE>`. Значения не задаются заранее — создаются исполнением сценария.

## Зависимости

- `<AA-ID>` — <короткое мнемоническое название, используемое в шагах этого TC>.

<Название придумывается при создании TC и должно дословно совпадать с тем, как Action называется в разделе «Описание сценария». Не описание назначения Action — короткое имя для ссылки. Если зависимостей нет — указать строго: «Зависимости отсутствуют.»>

## Описание сценария

<Каждый шаг, вызывающий Action, оформлять по формату: `Выполнить <Мнемоническое название> (`<AA-ID>`), используя <ссылка на группу тестовых данных>.` Код Action указывать при каждом вызове, включая повторные вызовы одной и той же Action в одном сценарии. Не расписывать в шаге отдельные поля/значения — вся информация о данных уже зафиксирована в разделе «Тестовые данные».>

1. Выполнить <Мнемоническое название> (`<AA-ID>`), используя <ссылка на группу тестовых данных>.
2. <Обычный шаг без вызова Action.>

## Условие проверки

<Однозначный ожидаемый бизнес-результат.>
